import { createServer } from "node:http";
import { EXTENSION_API_VERSION, EXTENSION_CONTRACTS } from "@ubeeq/extension-sdk";

const sendJson = (response, status, body) => { response.writeHead(status, { "content-type": "application/json; charset=utf-8" }); response.end(JSON.stringify(body)); };
const sendHtml = (response, html) => { response.writeHead(200, { "content-type": "text/html; charset=utf-8", "content-security-policy": "default-src 'self'; style-src 'unsafe-inline'; script-src 'unsafe-inline'; img-src 'self' data: http: https:" }); response.end(html); };
const apiUrl = (value) => { const url = new URL(value); if (!/^https?:$/.test(url.protocol)) throw new Error("Reference API URL must be HTTP(S)."); return url; };

const page = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Ubeeq reference workspace</title>
<style>
@import url('https://fonts.googleapis.com/css2?family=Barlow:wght@400;500;600;700&family=Barlow+Condensed:wght@500;600;700&display=swap');
:root{--canvas:#f2f2f3;--surface:transparent;--raised:#e9e9ea;--ink:#1d1f20;--muted:#5d5d60;--line:rgba(29,31,32,.16);--accent:#5980a6;--accent-deep:#416180;--tint:#eef6ff;--danger:#a6595c;--space:clamp(14px,2vw,28px)}*{box-sizing:border-box}body{margin:0;background:var(--canvas);color:var(--ink);font:15px/1.55 Barlow,system-ui,sans-serif}button,input{font:inherit}button{cursor:pointer}button:focus-visible,input:focus-visible{outline:2px solid var(--accent);outline-offset:2px}.skip{position:absolute;left:12px;top:-48px;background:var(--ink);color:var(--canvas);padding:8px 12px;z-index:4}.skip:focus{top:12px}.app{min-height:100vh;display:grid;grid-template-columns:242px minmax(0,1fr)}.topbar{grid-column:1/-1;display:flex;align-items:center;justify-content:space-between;gap:16px;min-height:58px;padding:0 var(--space);border-bottom:1px solid var(--line);background:var(--canvas)}.brandlock{display:flex;align-items:center;gap:10px;font:700 22px/1 'Barlow Condensed',sans-serif;letter-spacing:.03em;text-transform:uppercase}.brandmark{width:17px;height:17px;position:relative;border:1px solid var(--accent)}.brandmark:after{content:'';position:absolute;inset:4px;background:var(--accent)}.instance{font-size:12px;color:var(--muted);letter-spacing:.06em;text-transform:uppercase}.system{display:flex;align-items:center;gap:7px;font-size:12px;color:var(--muted)}.system:before{content:'';width:7px;height:7px;background:var(--accent);border-radius:50%}.rail{padding:24px 14px;border-right:1px solid var(--line)}.rail-label{margin:0 8px 8px;color:var(--muted);font-size:10px;font-weight:700;letter-spacing:.12em;text-transform:uppercase}.rail a{display:flex;justify-content:space-between;padding:9px 10px;margin:2px 0;color:var(--ink);text-decoration:none;font-size:14px}.rail a:first-of-type{border-left:2px solid var(--accent);background:var(--tint);font-weight:700}.rail small{color:var(--muted)}main{max-width:1180px;width:100%;padding:40px var(--space) 72px;margin:0 auto}main>header{display:flex;justify-content:space-between;align-items:flex-end;gap:24px;border-bottom:1px solid var(--line);padding-bottom:22px;margin-bottom:26px}h1,h2{font-family:'Barlow Condensed',sans-serif;letter-spacing:-.025em}h1{font-size:clamp(34px,5vw,52px);line-height:.95;margin:0 0 9px;text-transform:uppercase}h2{font-size:21px;margin:0 0 14px;text-transform:uppercase;letter-spacing:.02em}header p{max-width:600px;margin:0;color:var(--muted)}section{position:relative;border:1px solid var(--line);padding:22px;margin:18px 0;background:var(--surface)}section:before,section:after{content:'+';position:absolute;color:rgba(29,31,32,.52);font:500 16px/1 monospace}section:before{left:-6px;top:-8px}section:after{right:-6px;bottom:-8px}label{display:block;margin:9px 0;font-size:13px;font-weight:600}.row{display:flex;gap:12px;align-items:end;flex-wrap:wrap}.row label{flex:1;min-width:190px}input{display:block;width:100%;min-height:38px;margin-top:5px;padding:8px 10px;color:var(--ink);background:var(--canvas);border:1px solid var(--line);border-radius:0}input:hover{border-color:rgba(29,31,32,.45)}button{min-height:38px;padding:8px 14px;color:var(--ink);background:transparent;border:1px solid var(--line);border-radius:0;font:600 14px/1 'Barlow Condensed',sans-serif;letter-spacing:.035em;text-transform:uppercase}button:hover{background:var(--raised)}button#signup,button#createCreator,button#uploadFile,button#publish,button#export{color:var(--canvas);background:var(--accent);border-color:var(--accent)}button#signup:hover,button#createCreator:hover,button#uploadFile:hover,button#publish:hover,button#export:hover{background:var(--accent-deep);border-color:var(--accent-deep)}[role=status]{margin:9px 0 0;color:var(--accent-deep);font-size:13px}pre{min-height:120px;white-space:pre-wrap;background:var(--raised);border-left:2px solid var(--accent);padding:18px;overflow:auto;font:12px/1.6 ui-monospace,SFMono-Regular,Menlo,monospace}#output:before{content:'INSTANCE OUTPUT';display:block;margin-bottom:9px;color:var(--muted);font:700 10px/1 Barlow,sans-serif;letter-spacing:.12em}@media(max-width:800px){.app{display:block}.rail{display:flex;overflow:auto;gap:2px;padding:8px;border-bottom:1px solid var(--line);border-right:0}.rail-label{display:none}.rail a{white-space:nowrap}.topbar{min-height:52px}.instance{display:none}main{padding-top:28px}main>header{display:block}.row label{min-width:100%}section{padding:18px}button{width:100%}.row button{width:auto}}
</style></head>
<body><a class="skip" href="#workspace">Skip to workspace</a><div class="app"><header class="topbar"><div class="brandlock"><span class="brandmark" aria-hidden="true"></span>Ubeeq</div><div class="instance">Reference instance · local-eu-1</div><div class="system">Diagnostics OK</div></header><nav class="rail" aria-label="Reference workspace"><p class="rail-label">Creator</p><a href="#workspace">Works <small>local</small></a><a href="#upload">Assets <small>0</small></a><a href="#portability">Collections <small>—</small></a><p class="rail-label">Instance</p><a href="#portability">Export / import</a><a href="#output">Activity</a></nav><main id="workspace"><header><div><p class="instance">Portable creator workspace</p><h1>Works</h1><p>A neutral, self-hostable reference interface. It deliberately contains no product branding, discovery, pricing, or policy rules.</p></div><button type="button" onclick="document.getElementById('title').focus()">New Work</button></header>
<section><h2>Sign in</h2><div class="row"><label>Email <input id="email" type="email" value="creator@example.test"></label><label>Password <input id="password" type="password" value="a-safe-local-password"></label></div><button id="signup">Create local account</button> <button id="signin">Sign in</button><p id="identity" role="status"></p></section>
<section><h2>Creator profile and work</h2><div class="row"><label>Handle <input id="handle" value="creator"></label><label>Display name <input id="display" value="Creator"></label></div><button id="createCreator">Create profile</button><div class="row"><label>Work title <input id="title" value="A local work"></label><button id="createWork">Create work</button></div><p id="work" role="status"></p></section>
<section id="upload"><h2>Upload and publish</h2><label>Image file <input id="file" type="file" accept="image/*"></label><div class="row"><button id="uploadFile">Upload selected file</button> <button id="process">Run local processing job</button> <button id="publish">Publish current work</button></div><p id="uploadStatus" role="status"></p></section>
<section id="portability"><h2>Portability</h2><div class="row"><button id="export">Download creator manifest</button><label>Import manifest <input id="importFile" type="file" accept="application/json"></label><button id="validate">Validate import</button> <button id="import">Import manifest</button></div><p>Manifest imports are metadata-only. Original files require a separate transfer and processing step.</p></section>
<section><h2>Public work</h2><label>Work ID <input id="publicId"></label><button id="viewPublic">View public response</button></section><pre id="output" aria-live="polite">Ready.</pre></main>
<script>
let token=localStorage.getItem('ubeeq.reference.token')||'',workId=localStorage.getItem('ubeeq.reference.work')||'';
const $=id=>document.getElementById(id),out=value=>{$('output').textContent=typeof value==='string'?value:JSON.stringify(value,null,2)},auth=()=>token?{authorization:'Bearer '+token}:{};
const api=async(path,method='GET',body)=>{const response=await fetch('/api'+path,{method,headers:{'content-type':'application/json',...auth()},body:body?JSON.stringify(body):undefined});const value=response.status===204?null:await response.json();if(!response.ok)throw new Error(value.error?.message||'Request failed');return value};
const account=()=>({email:$('email').value,password:$('password').value});
$('signup').onclick=async()=>{try{out(await api('/v1/auth/sign-up','POST',account()))}catch(e){out(e.message)}};
$('signin').onclick=async()=>{try{const result=await api('/v1/auth/sign-in','POST',account());token=result.token;localStorage.setItem('ubeeq.reference.token',token);$('identity').textContent='Signed in.';out(result)}catch(e){out(e.message)}};
$('createCreator').onclick=async()=>{try{out(await api('/v1/creators','POST',{handle:$('handle').value,displayName:$('display').value}))}catch(e){out(e.message)}};
$('createWork').onclick=async()=>{try{const result=await api('/v1/works','POST',{title:$('title').value});workId=result.work.id;localStorage.setItem('ubeeq.reference.work',workId);$('work').textContent='Current work: '+workId;out(result)}catch(e){out(e.message)}};
$('uploadFile').onclick=async()=>{try{if(!workId)throw new Error('Create a Work first.');const file=$('file').files[0];if(!file)throw new Error('Choose an image file.');const bytes=new Uint8Array(await file.arrayBuffer()),digestBytes=new Uint8Array(await crypto.subtle.digest('SHA-256',bytes)),hash=Array.from(digestBytes).map(b=>b.toString(16).padStart(2,'0')).join(''),checksumBase64=btoa(String.fromCharCode(...digestBytes));const upload=await api('/v1/uploads','POST',{workId,mimeType:file.type||'application/octet-stream',byteLength:file.size,checksum:hash}),directUrl=upload.upload.parts?.[0]?.url;if(directUrl){const direct=await fetch(directUrl,{method:'PUT',headers:{'content-type':file.type||'application/octet-stream','x-amz-checksum-sha256':checksumBase64,'x-amz-meta-checksum':hash,'x-amz-meta-scope':'private','x-amz-meta-bytelength':String(file.size)},body:bytes});if(!direct.ok)throw new Error('Direct object upload failed: '+direct.status)}else{let binary='';bytes.forEach(b=>binary+=String.fromCharCode(b));await api('/v1/uploads/'+upload.upload.uploadId+'/content','PUT',{base64:btoa(binary)})}const complete=await api('/v1/uploads/'+upload.upload.uploadId+'/complete','POST',{workId,checksum:hash,byteLength:file.size});$('uploadStatus').textContent='Queued for processing.';out(complete)}catch(e){out(e.message)}};
$('process').onclick=async()=>{try{out(await api('/v1/operations/jobs/run-next','POST',{}))}catch(e){out(e.message)}};
$('publish').onclick=async()=>{try{if(!workId)throw new Error('Create a Work first.');const result=await api('/v1/works/'+workId+'/publications','POST',{destination:'local'});$('publicId').value=workId;out(result)}catch(e){out(e.message)}};
$('export').onclick=async()=>{try{const manifest=await api('/v1/exports/me');const link=Object.assign(document.createElement('a'),{href:URL.createObjectURL(new Blob([JSON.stringify(manifest,null,2)],{type:'application/json'})),download:'ubeeq-creator-export.json'});link.click();URL.revokeObjectURL(link.href);out({checksum:manifest.checksum,secretsExcluded:manifest.secretsExcluded})}catch(e){out(e.message)}};
const readImport=async()=>JSON.parse(await $('importFile').files[0].text());
$('validate').onclick=async()=>{try{out(await api('/v1/imports/validate','POST',{manifest:await readImport()}))}catch(e){out(e.message)}};
$('import').onclick=async()=>{try{out(await api('/v1/imports','POST',{manifest:await readImport(),dryRun:false}))}catch(e){out(e.message)}};
$('viewPublic').onclick=async()=>{try{out(await api('/v1/public/works/'+$('publicId').value))}catch(e){out(e.message)}};
if(workId){$('work').textContent='Current work: '+workId;$('publicId').value=workId}if(token)$('identity').textContent='Saved local session available.';
</script></main></div></body></html>`;

export const createReferenceHandler = ({ referenceApiUrl = process.env.UBEEQ_REFERENCE_API_URL || "http://127.0.0.1:4100" } = {}) => {
  const upstream = apiUrl(referenceApiUrl);
  return async (request, response) => {
    const url = new URL(request.url || "/", "http://reference.local");
    if (url.pathname === "/health") return sendJson(response, 200, { status: "ok" });
    if (url.pathname === "/extension-contracts") return sendJson(response, 200, { apiVersion: EXTENSION_API_VERSION, contracts: EXTENSION_CONTRACTS });
    if (url.pathname === "/api-configuration") return sendJson(response, 200, { apiPath: "/api", referenceApiUrl: upstream.origin });
    if (url.pathname.startsWith("/api/")) {
      try {
        const body = await new Promise((resolve, reject) => { const chunks=[]; request.on("data", chunk=>chunks.push(chunk)); request.on("end",()=>resolve(Buffer.concat(chunks))); request.on("error",reject); });
        const target = new URL(url.pathname.slice(4) + url.search, upstream);
        const headers = { "content-type": request.headers["content-type"] || "application/json", ...(request.headers.authorization ? { authorization: request.headers.authorization } : {}), ...(request.headers["idempotency-key"] ? { "idempotency-key": request.headers["idempotency-key"] } : {}) };
        const upstreamResponse = await fetch(target, { method: request.method, headers, body: body.length ? body : undefined });
        response.writeHead(upstreamResponse.status, { "content-type": upstreamResponse.headers.get("content-type") || "application/json" }); response.end(Buffer.from(await upstreamResponse.arrayBuffer())); return;
      } catch { return sendJson(response, 502, { error: "reference_api_unavailable" }); }
    }
    if (url.pathname === "/" || url.pathname === "/workspace") return sendHtml(response, page);
    return sendJson(response, 404, { error: "not_found" });
  };
};

export const createReferenceApp = (configuration = {}) => createServer(createReferenceHandler(configuration));
if (import.meta.url === `file://${process.argv[1]}`) { const port = Number(process.env.PORT || 4173); createReferenceApp().listen(port, "127.0.0.1", () => console.log(`Ubeeq reference workspace listening on http://127.0.0.1:${port}`)); }
