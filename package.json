{
  "name": "ubeeq",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "npm run check:public-boundary && npm run build --workspace @ubeeq/core && npm run build --workspace @ubeeq/extension-sdk && npm run build --workspace @ubeeq/api",
    "check:public-boundary": "node scripts/check-public-boundary.mjs",
    "test": "npm run --workspaces --if-present test"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
