# Machine scalable multi-cell

Future provider-neutral profile for independent scalable machine cells plus an optional routing and migration control plane. Cells retain separate mutable data, credentials, queues, and backups.
