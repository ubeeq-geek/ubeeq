apiVersion: apps/v1
kind: Deployment
metadata:
  name: ubeeq-web
  labels:
    app.kubernetes.io/name: ubeeq-web
    app.kubernetes.io/part-of: ubeeq
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: ubeeq-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ubeeq-web
        app.kubernetes.io/part-of: ubeeq
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: web
          image: ghcr.io/ubeeq/ubeeq-reference
          command: ["node", "apps/web-reference/src/server.mjs"]
          ports: [{ name: http, containerPort: 4173 }]
          env:
            - { name: PORT, value: "4173" }
            - { name: UBEEQ_REFERENCE_API_URL, value: http://ubeeq-api }
          readinessProbe: { httpGet: { path: /health, port: http }, initialDelaySeconds: 5, periodSeconds: 10 }
          livenessProbe: { httpGet: { path: /health, port: http }, initialDelaySeconds: 15, periodSeconds: 20 }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
---
apiVersion: v1
kind: Service
metadata:
  name: ubeeq-web
spec:
  selector:
    app.kubernetes.io/name: ubeeq-web
  ports:
    - { name: http, port: 80, targetPort: http }
