{
  "name": "@ubeeq/media-ffmpeg",
  "version": "0.1.0",
  "private": false,
  "publishConfig": { "access": "public" },
  "files": ["dist", "README.md"],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc -p tsconfig.json", "test:contract": "npm run build && node --test test/*.test.mjs", "test": "npm run test:contract" },
  "dependencies": { "@ubeeq/processing": "^0.1.0" }
}
